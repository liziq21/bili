name: components
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: "^3.13.4"
  flutter: ">=3.47.5"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

